version: '3'

services:
  techblog:
    build:
      dockerfile: Dockerfile.build
      context: .
    environment:
        - JEKYLL_ENV=docker
    command: jekyll serve --force_polling --incremental 
    ports:
        - 4000:4000
        - 35729:35729
    volumes:
        - ./:/srv/jekyll